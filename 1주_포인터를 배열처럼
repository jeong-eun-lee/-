#include <stdio.h>
#include <stdlib.h>

int main()
{
    // 배열의 이름 = 포인터, 포인터를 배열이름처럼 사용가능.
    int arr[3]= {1,3,5};
    int *ptr;

    ptr= arr;   // arr은 포인터상수,  arr=ptr은 불가.
    printf("ptr[0]: %d ", ptr[0]);

    return 0;
}
